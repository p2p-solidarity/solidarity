import { Redirect } from 'expo-router';

/** Retired personal identity prototype kept for old links. */
export default function PersonalIdentityCompatibilityRoute() {
  return <Redirect href="/id/zk-settings" />;
}
