///
/// HybridSpruceDidSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `SpruceDidEvent` to properly resolve imports.
namespace margelo::nitro::solidarity::sprucedid { struct SpruceDidEvent; }

#include <string>
#include <NitroModules/Promise.hpp>
#include <NitroModules/ArrayBuffer.hpp>
#include <functional>
#include "SpruceDidEvent.hpp"

namespace margelo::nitro::solidarity::sprucedid {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `SpruceDid`
   * Inherit this class to create instances of `HybridSpruceDidSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridSpruceDid: public HybridSpruceDidSpec {
   * public:
   *   HybridSpruceDid(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridSpruceDidSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridSpruceDidSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridSpruceDidSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual std::shared_ptr<Promise<std::string>> generateKey(const std::string& alias, const std::string& keyType, bool requireBiometric) = 0;
      virtual bool hasKey(const std::string& alias) = 0;
      virtual std::shared_ptr<Promise<std::string>> keyAuthMode(const std::string& alias) = 0;
      virtual std::shared_ptr<Promise<bool>> deleteKey(const std::string& alias) = 0;
      virtual std::shared_ptr<Promise<std::string>> listSyncableP256Keys(const std::string& alias) = 0;
      virtual std::shared_ptr<Promise<bool>> deleteSyncableP256Key(const std::string& alias, const std::string& labelHex) = 0;
      virtual std::shared_ptr<Promise<std::string>> getPublicKeyJwk(const std::string& alias) = 0;
      virtual std::shared_ptr<Promise<std::string>> signJws(const std::string& alias, const std::shared_ptr<ArrayBuffer>& payload) = 0;
      virtual std::shared_ptr<Promise<std::shared_ptr<ArrayBuffer>>> signRawP256(const std::string& alias, const std::shared_ptr<ArrayBuffer>& digest) = 0;
      virtual std::function<void()> addEventListener(const std::function<void(const SpruceDidEvent& /* event */)>& handler) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "SpruceDid";
  };

} // namespace margelo::nitro::solidarity::sprucedid
