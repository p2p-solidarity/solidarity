# Generated by Cargo
target/

# uniffi-bindgen scratch output from build-ios.sh / build-android.sh.
# The deployed copies live at:
#   nitro-modules/semaphore/mopro/ios/mopro.swift           (iOS Swift)
#   nitro-modules/semaphore/android/src/main/java/uniffi/   (Android Kotlin)
# so this directory is a regenerable staging area only.
bindings/

# Lockfile — for a library crate we don't pin it, so it can drift between
# host and CI without causing diffs. (If we ever publish this crate or
# need reproducible builds, drop this line.)
Cargo.lock
