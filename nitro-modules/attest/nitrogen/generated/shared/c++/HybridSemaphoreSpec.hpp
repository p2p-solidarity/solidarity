///
/// HybridSemaphoreSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `SemaphoreProof` to properly resolve imports.
namespace margelo::nitro::solidarity::attest { struct SemaphoreProof; }

#include <string>
#include <NitroModules/Promise.hpp>
#include <NitroModules/ArrayBuffer.hpp>
#include <vector>
#include "SemaphoreProof.hpp"

namespace margelo::nitro::solidarity::attest {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `Semaphore`
   * Inherit this class to create instances of `HybridSemaphoreSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridSemaphore: public HybridSemaphoreSpec {
   * public:
   *   HybridSemaphore(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridSemaphoreSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridSemaphoreSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridSemaphoreSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual std::shared_ptr<Promise<std::string>> generateIdentity() = 0;
      virtual std::shared_ptr<Promise<std::string>> identityFromSeed(const std::shared_ptr<ArrayBuffer>& seed) = 0;
      virtual std::string getCommitment() = 0;
      virtual std::shared_ptr<Promise<bool>> loadIdentityFromKeychain(const std::string& alias) = 0;
      virtual std::shared_ptr<Promise<void>> storeIdentityToKeychain(const std::string& alias) = 0;
      virtual std::shared_ptr<Promise<void>> deleteIdentity() = 0;
      virtual std::shared_ptr<Promise<std::shared_ptr<ArrayBuffer>>> exportPrivateKey() = 0;
      virtual std::shared_ptr<Promise<std::string>> importPrivateKey(const std::shared_ptr<ArrayBuffer>& bytes) = 0;
      virtual std::shared_ptr<Promise<std::string>> groupRootFromCommitments(const std::vector<std::string>& commitments) = 0;
      virtual std::shared_ptr<Promise<SemaphoreProof>> generateProof(const std::vector<std::string>& commitments, const std::string& scope, const std::string& signal) = 0;
      virtual std::shared_ptr<Promise<bool>> verifyProof(const SemaphoreProof& proof, double merkleTreeDepth) = 0;
      virtual std::string extractNullifier(const SemaphoreProof& proof) = 0;
      virtual bool hasNullifier(const std::string& scope, const std::string& nullifier) = 0;
      virtual void recordNullifier(const std::string& scope, const std::string& nullifier) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "Semaphore";
  };

} // namespace margelo::nitro::solidarity::attest
