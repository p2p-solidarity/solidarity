///
/// HybridPasskeyPrfSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `PasskeyPrfResult` to properly resolve imports.
namespace margelo::nitro::solidarity::keystone { struct PasskeyPrfResult; }

#include "PasskeyPrfResult.hpp"
#include <NitroModules/Promise.hpp>
#include <string>
#include <vector>

namespace margelo::nitro::solidarity::keystone {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `PasskeyPrf`
   * Inherit this class to create instances of `HybridPasskeyPrfSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridPasskeyPrf: public HybridPasskeyPrfSpec {
   * public:
   *   HybridPasskeyPrf(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridPasskeyPrfSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridPasskeyPrfSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridPasskeyPrfSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual bool isSupported() = 0;
      virtual std::shared_ptr<Promise<PasskeyPrfResult>> createCredential(const std::string& rpId, const std::string& userName, const std::string& userId, const std::string& prfInput, const std::vector<std::string>& excludeCredentialIds) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "PasskeyPrf";
  };

} // namespace margelo::nitro::solidarity::keystone
