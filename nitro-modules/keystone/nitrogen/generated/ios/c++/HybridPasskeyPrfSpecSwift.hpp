///
/// HybridPasskeyPrfSpecSwift.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#include "HybridPasskeyPrfSpec.hpp"

// Forward declaration of `HybridPasskeyPrfSpec_cxx` to properly resolve imports.
namespace Keystone { class HybridPasskeyPrfSpec_cxx; }

// Forward declaration of `PasskeyPrfResult` to properly resolve imports.
namespace margelo::nitro::solidarity::keystone { struct PasskeyPrfResult; }

#include "PasskeyPrfResult.hpp"
#include <NitroModules/Promise.hpp>
#include <string>
#include <optional>
#include <vector>

#include "Keystone-Swift-Cxx-Umbrella.hpp"

namespace margelo::nitro::solidarity::keystone {

  /**
   * The C++ part of HybridPasskeyPrfSpec_cxx.swift.
   *
   * HybridPasskeyPrfSpecSwift (C++) accesses HybridPasskeyPrfSpec_cxx (Swift), and might
   * contain some additional bridging code for C++ <> Swift interop.
   *
   * Since this obviously introduces an overhead, I hope at some point in
   * the future, HybridPasskeyPrfSpec_cxx can directly inherit from the C++ class HybridPasskeyPrfSpec
   * to simplify the whole structure and memory management.
   */
  class HybridPasskeyPrfSpecSwift: public virtual HybridPasskeyPrfSpec {
  public:
    // Constructor from a Swift instance
    explicit HybridPasskeyPrfSpecSwift(const Keystone::HybridPasskeyPrfSpec_cxx& swiftPart):
      HybridObject(HybridPasskeyPrfSpec::TAG),
      _swiftPart(swiftPart) { }

  public:
    // Get the Swift part
    inline Keystone::HybridPasskeyPrfSpec_cxx& getSwiftPart() noexcept {
      return _swiftPart;
    }

  public:
    inline size_t getExternalMemorySize() noexcept override {
      return _swiftPart.getMemorySize();
    }
    bool equals(const std::shared_ptr<HybridObject>& other) override {
      if (auto otherCast = std::dynamic_pointer_cast<HybridPasskeyPrfSpecSwift>(other)) {
        return _swiftPart.equals(otherCast->_swiftPart);
      }
      return false;
    }
    void dispose() noexcept override {
      _swiftPart.dispose();
    }
    std::string toString() override {
      return _swiftPart.toString();
    }

  public:
    // Properties
    

  public:
    // Methods
    inline bool isSupported() override {
      auto __result = _swiftPart.isSupported();
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
      auto __value = std::move(__result.value());
      return __value;
    }
    inline std::shared_ptr<Promise<PasskeyPrfResult>> createCredential(const std::string& rpId, const std::string& userName, const std::string& userId, const std::string& prfInput, const std::vector<std::string>& excludeCredentialIds) override {
      auto __result = _swiftPart.createCredential(rpId, userName, userId, prfInput, excludeCredentialIds);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
      auto __value = std::move(__result.value());
      return __value;
    }

  private:
    Keystone::HybridPasskeyPrfSpec_cxx _swiftPart;
  };

} // namespace margelo::nitro::solidarity::keystone
