{
  "name": "@solidarity/nitro-airdrop",
  "version": "0.1.0",
  "private": true,
  "description": "Nitro module wrapping Apple's AirDrop activity via UIActivityViewController. iOS-only feature; Android side is a clean stub that surfaces NotImplementedError so the JS surface stays consistent.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "nitrogen": "nitrogen",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit"
  },
  "react-native": {
    "supportedPlatforms": ["ios", "android"]
  },
  "peerDependencies": {
    "react-native-nitro-modules": "*"
  },
  "devDependencies": {
    "react-native-nitro-modules": "^0.35.7"
  }
}
