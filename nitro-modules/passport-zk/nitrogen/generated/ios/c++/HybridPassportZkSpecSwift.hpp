///
/// HybridPassportZkSpecSwift.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#include "HybridPassportZkSpec.hpp"

// Forward declaration of `HybridPassportZkSpec_cxx` to properly resolve imports.
namespace PassportZK { class HybridPassportZkSpec_cxx; }

// Forward declaration of `NoirProofResult` to properly resolve imports.
namespace margelo::nitro::margelo::solidarity::passportzk { struct NoirProofResult; }
// Forward declaration of `ArrayBufferHolder` to properly resolve imports.
namespace NitroModules { class ArrayBufferHolder; }

#include "NoirProofResult.hpp"
#include <NitroModules/Promise.hpp>
#include <NitroModules/ArrayBuffer.hpp>
#include <NitroModules/ArrayBufferHolder.hpp>
#include <string>
#include <optional>

#include "PassportZK-Swift-Cxx-Umbrella.hpp"

namespace margelo::nitro::margelo::solidarity::passportzk {

  /**
   * The C++ part of HybridPassportZkSpec_cxx.swift.
   *
   * HybridPassportZkSpecSwift (C++) accesses HybridPassportZkSpec_cxx (Swift), and might
   * contain some additional bridging code for C++ <> Swift interop.
   *
   * Since this obviously introduces an overhead, I hope at some point in
   * the future, HybridPassportZkSpec_cxx can directly inherit from the C++ class HybridPassportZkSpec
   * to simplify the whole structure and memory management.
   */
  class HybridPassportZkSpecSwift: public virtual HybridPassportZkSpec {
  public:
    // Constructor from a Swift instance
    explicit HybridPassportZkSpecSwift(const PassportZK::HybridPassportZkSpec_cxx& swiftPart):
      HybridObject(HybridPassportZkSpec::TAG),
      _swiftPart(swiftPart) { }

  public:
    // Get the Swift part
    inline PassportZK::HybridPassportZkSpec_cxx& getSwiftPart() noexcept {
      return _swiftPart;
    }

  public:
    inline size_t getExternalMemorySize() noexcept override {
      return _swiftPart.getMemorySize();
    }
    bool equals(const std::shared_ptr<HybridObject>& other) override {
      if (auto otherCast = std::dynamic_pointer_cast<HybridPassportZkSpecSwift>(other)) {
        return _swiftPart.equals(otherCast->_swiftPart);
      }
      return false;
    }
    void dispose() noexcept override {
      _swiftPart.dispose();
    }
    std::string toString() override {
      return _swiftPart.toString();
    }

  public:
    // Properties
    

  public:
    // Methods
    inline std::shared_ptr<Promise<NoirProofResult>> generateNoirProof(const std::string& circuitPath, const std::optional<std::string>& srsPath, const std::string& inputsJson) override {
      auto __result = _swiftPart.generateNoirProof(circuitPath, srsPath, inputsJson);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
      auto __value = std::move(__result.value());
      return __value;
    }
    inline std::shared_ptr<Promise<std::shared_ptr<ArrayBuffer>>> getNoirVerificationKey(const std::string& circuitPath, const std::optional<std::string>& srsPath) override {
      auto __result = _swiftPart.getNoirVerificationKey(circuitPath, srsPath);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
      auto __value = std::move(__result.value());
      return __value;
    }
    inline std::shared_ptr<Promise<bool>> verifyNoirProof(const std::shared_ptr<ArrayBuffer>& proof, const std::shared_ptr<ArrayBuffer>& vk) override {
      auto __result = _swiftPart.verifyNoirProof(ArrayBufferHolder(proof), ArrayBufferHolder(vk));
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
      auto __value = std::move(__result.value());
      return __value;
    }

  private:
    PassportZK::HybridPassportZkSpec_cxx _swiftPart;
  };

} // namespace margelo::nitro::margelo::solidarity::passportzk
