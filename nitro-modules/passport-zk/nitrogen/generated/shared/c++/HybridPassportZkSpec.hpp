///
/// HybridPassportZkSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `NoirProofResult` to properly resolve imports.
namespace margelo::nitro::margelo::solidarity::passportzk { struct NoirProofResult; }

#include "NoirProofResult.hpp"
#include <NitroModules/Promise.hpp>
#include <string>
#include <optional>
#include <NitroModules/ArrayBuffer.hpp>

namespace margelo::nitro::margelo::solidarity::passportzk {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `PassportZk`
   * Inherit this class to create instances of `HybridPassportZkSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridPassportZk: public HybridPassportZkSpec {
   * public:
   *   HybridPassportZk(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridPassportZkSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridPassportZkSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridPassportZkSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual std::shared_ptr<Promise<NoirProofResult>> generateNoirProof(const std::string& circuitPath, const std::optional<std::string>& srsPath, const std::string& inputsJson) = 0;
      virtual std::shared_ptr<Promise<std::shared_ptr<ArrayBuffer>>> getNoirVerificationKey(const std::string& circuitPath, const std::optional<std::string>& srsPath) = 0;
      virtual std::shared_ptr<Promise<bool>> verifyNoirProof(const std::shared_ptr<ArrayBuffer>& proof, const std::shared_ptr<ArrayBuffer>& vk) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "PassportZk";
  };

} // namespace margelo::nitro::margelo::solidarity::passportzk
