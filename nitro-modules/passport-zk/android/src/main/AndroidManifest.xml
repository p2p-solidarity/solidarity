<?xml version="1.0" encoding="utf-8"?>
<!--
  AndroidManifest.xml
  @solidarity/nitro-passport-zk (Android)

  Empty manifest — the Rust passport_zk_mopro cdylib is loaded at runtime
  via System.loadLibrary; no Android permissions or features needed. The
  host app's manifest already declares NFC + anything else it needs.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android" />
