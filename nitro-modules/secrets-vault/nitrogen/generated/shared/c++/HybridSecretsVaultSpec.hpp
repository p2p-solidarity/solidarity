///
/// HybridSecretsVaultSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `EnsureWrappingKeyResult` to properly resolve imports.
namespace margelo::nitro::solidarity::secretsvault { struct EnsureWrappingKeyResult; }
// Forward declaration of `WrappedSecret` to properly resolve imports.
namespace margelo::nitro::solidarity::secretsvault { struct WrappedSecret; }

#include "EnsureWrappingKeyResult.hpp"
#include <NitroModules/Promise.hpp>
#include <string>
#include "WrappedSecret.hpp"
#include <NitroModules/ArrayBuffer.hpp>

namespace margelo::nitro::solidarity::secretsvault {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `SecretsVault`
   * Inherit this class to create instances of `HybridSecretsVaultSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridSecretsVault: public HybridSecretsVaultSpec {
   * public:
   *   HybridSecretsVault(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridSecretsVaultSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridSecretsVaultSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridSecretsVaultSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual bool isHardwareAvailable() = 0;
      virtual std::shared_ptr<Promise<EnsureWrappingKeyResult>> ensureWrappingKey(const std::string& keyAlias, bool requireBiometric) = 0;
      virtual std::shared_ptr<Promise<WrappedSecret>> wrap(const std::string& keyAlias, const std::shared_ptr<ArrayBuffer>& plaintext) = 0;
      virtual std::shared_ptr<Promise<std::shared_ptr<ArrayBuffer>>> unwrap(const WrappedSecret& wrapped) = 0;
      virtual std::shared_ptr<Promise<void>> deleteKey(const std::string& keyAlias) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "SecretsVault";
  };

} // namespace margelo::nitro::solidarity::secretsvault
