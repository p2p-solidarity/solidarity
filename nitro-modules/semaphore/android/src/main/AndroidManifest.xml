<?xml version="1.0" encoding="utf-8"?>
<!--
  AndroidManifest.xml
  @solidarity/nitro-semaphore (Android)

  This manifest is intentionally minimal: it only declares the package.
  Identity material is stored via EncryptedSharedPreferences (which
  requires no extra permissions on Android), and the JNI .so is loaded
  via System.loadLibrary inside the Kotlin impl.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android" />
