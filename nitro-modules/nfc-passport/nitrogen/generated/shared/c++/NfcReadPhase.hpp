///
/// NfcReadPhase.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::solidarity::nfcpassport {

  /**
   * An enum which can be represented as a JavaScript union (NfcReadPhase).
   */
  enum class NfcReadPhase {
    CONNECTING      SWIFT_NAME(connecting) = 0,
    AUTHENTICATING      SWIFT_NAME(authenticating) = 1,
    READING_DG      SWIFT_NAME(readingDg) = 2,
    VERIFYING      SWIFT_NAME(verifying) = 3,
    DONE      SWIFT_NAME(done) = 4,
    ERROR      SWIFT_NAME(error) = 5,
  } CLOSED_ENUM;

} // namespace margelo::nitro::solidarity::nfcpassport

namespace margelo::nitro {

  // C++ NfcReadPhase <> JS NfcReadPhase (union)
  template <>
  struct JSIConverter<margelo::nitro::solidarity::nfcpassport::NfcReadPhase> final {
    static inline margelo::nitro::solidarity::nfcpassport::NfcReadPhase fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("connecting"): return margelo::nitro::solidarity::nfcpassport::NfcReadPhase::CONNECTING;
        case hashString("authenticating"): return margelo::nitro::solidarity::nfcpassport::NfcReadPhase::AUTHENTICATING;
        case hashString("reading-dg"): return margelo::nitro::solidarity::nfcpassport::NfcReadPhase::READING_DG;
        case hashString("verifying"): return margelo::nitro::solidarity::nfcpassport::NfcReadPhase::VERIFYING;
        case hashString("done"): return margelo::nitro::solidarity::nfcpassport::NfcReadPhase::DONE;
        case hashString("error"): return margelo::nitro::solidarity::nfcpassport::NfcReadPhase::ERROR;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum NfcReadPhase - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::solidarity::nfcpassport::NfcReadPhase arg) {
      switch (arg) {
        case margelo::nitro::solidarity::nfcpassport::NfcReadPhase::CONNECTING: return JSIConverter<std::string>::toJSI(runtime, "connecting");
        case margelo::nitro::solidarity::nfcpassport::NfcReadPhase::AUTHENTICATING: return JSIConverter<std::string>::toJSI(runtime, "authenticating");
        case margelo::nitro::solidarity::nfcpassport::NfcReadPhase::READING_DG: return JSIConverter<std::string>::toJSI(runtime, "reading-dg");
        case margelo::nitro::solidarity::nfcpassport::NfcReadPhase::VERIFYING: return JSIConverter<std::string>::toJSI(runtime, "verifying");
        case margelo::nitro::solidarity::nfcpassport::NfcReadPhase::DONE: return JSIConverter<std::string>::toJSI(runtime, "done");
        case margelo::nitro::solidarity::nfcpassport::NfcReadPhase::ERROR: return JSIConverter<std::string>::toJSI(runtime, "error");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert NfcReadPhase to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("connecting"):
        case hashString("authenticating"):
        case hashString("reading-dg"):
        case hashString("verifying"):
        case hashString("done"):
        case hashString("error"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
