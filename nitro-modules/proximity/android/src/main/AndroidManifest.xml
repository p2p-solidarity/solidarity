<?xml version="1.0" encoding="utf-8"?>
<!--
  AndroidManifest.xml
  @solidarity/nitro-proximity (Android)

  This manifest is intentionally minimal: it only declares the package.
  Runtime permissions (BLUETOOTH_ADVERTISE / CONNECT / SCAN / UWB_RANGING)
  must be declared by the *consuming app's* manifest (apps/expo/app.json
  already lists them), because the merger respects the strictest set.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android" />
