///
/// HybridProximitySpecSwift.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#include "HybridProximitySpec.hpp"

// Forward declaration of `HybridProximitySpec_cxx` to properly resolve imports.
namespace SolidarityProximity { class HybridProximitySpec_cxx; }

// Forward declaration of `ArrayBufferHolder` to properly resolve imports.
namespace NitroModules { class ArrayBufferHolder; }
// Forward declaration of `ProximityEvent` to properly resolve imports.
namespace margelo::nitro::solidarity::proximity { struct ProximityEvent; }
// Forward declaration of `ProximityEventKind` to properly resolve imports.
namespace margelo::nitro::solidarity::proximity { enum class ProximityEventKind; }
// Forward declaration of `ProximityPeer` to properly resolve imports.
namespace margelo::nitro::solidarity::proximity { struct ProximityPeer; }
// Forward declaration of `ProximityDirection` to properly resolve imports.
namespace margelo::nitro::solidarity::proximity { struct ProximityDirection; }

#include <string>
#include <NitroModules/Promise.hpp>
#include <NitroModules/ArrayBuffer.hpp>
#include <NitroModules/ArrayBufferHolder.hpp>
#include <functional>
#include "ProximityEvent.hpp"
#include "ProximityEventKind.hpp"
#include "ProximityPeer.hpp"
#include <optional>
#include "ProximityDirection.hpp"

#include "SolidarityProximity-Swift-Cxx-Umbrella.hpp"

namespace margelo::nitro::solidarity::proximity {

  /**
   * The C++ part of HybridProximitySpec_cxx.swift.
   *
   * HybridProximitySpecSwift (C++) accesses HybridProximitySpec_cxx (Swift), and might
   * contain some additional bridging code for C++ <> Swift interop.
   *
   * Since this obviously introduces an overhead, I hope at some point in
   * the future, HybridProximitySpec_cxx can directly inherit from the C++ class HybridProximitySpec
   * to simplify the whole structure and memory management.
   */
  class HybridProximitySpecSwift: public virtual HybridProximitySpec {
  public:
    // Constructor from a Swift instance
    explicit HybridProximitySpecSwift(const SolidarityProximity::HybridProximitySpec_cxx& swiftPart):
      HybridObject(HybridProximitySpec::TAG),
      _swiftPart(swiftPart) { }

  public:
    // Get the Swift part
    inline SolidarityProximity::HybridProximitySpec_cxx& getSwiftPart() noexcept {
      return _swiftPart;
    }

  public:
    inline size_t getExternalMemorySize() noexcept override {
      return _swiftPart.getMemorySize();
    }
    bool equals(const std::shared_ptr<HybridObject>& other) override {
      if (auto otherCast = std::dynamic_pointer_cast<HybridProximitySpecSwift>(other)) {
        return _swiftPart.equals(otherCast->_swiftPart);
      }
      return false;
    }
    void dispose() noexcept override {
      _swiftPart.dispose();
    }
    std::string toString() override {
      return _swiftPart.toString();
    }

  public:
    // Properties
    

  public:
    // Methods
    inline void startAdvertising(const std::string& displayName, const std::string& serviceType, const std::string& discoveryInfoJson) override {
      auto __result = _swiftPart.startAdvertising(displayName, serviceType, discoveryInfoJson);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void stopAdvertising() override {
      auto __result = _swiftPart.stopAdvertising();
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void startBrowsing(const std::string& serviceType) override {
      auto __result = _swiftPart.startBrowsing(serviceType);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void stopBrowsing() override {
      auto __result = _swiftPart.stopBrowsing();
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline std::shared_ptr<Promise<bool>> invitePeer(const std::string& peerId, const std::shared_ptr<ArrayBuffer>& payload, double timeoutSec) override {
      auto __result = _swiftPart.invitePeer(peerId, ArrayBufferHolder(payload), std::forward<decltype(timeoutSec)>(timeoutSec));
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
      auto __value = std::move(__result.value());
      return __value;
    }
    inline void acceptInvitation(const std::string& peerId) override {
      auto __result = _swiftPart.acceptInvitation(peerId);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void rejectInvitation(const std::string& peerId) override {
      auto __result = _swiftPart.rejectInvitation(peerId);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline std::shared_ptr<Promise<void>> sendData(const std::string& peerId, const std::shared_ptr<ArrayBuffer>& data) override {
      auto __result = _swiftPart.sendData(peerId, ArrayBufferHolder(data));
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
      auto __value = std::move(__result.value());
      return __value;
    }
    inline void disconnect(const std::string& peerId) override {
      auto __result = _swiftPart.disconnect(peerId);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline std::shared_ptr<Promise<void>> startRanging(const std::string& peerId) override {
      auto __result = _swiftPart.startRanging(peerId);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
      auto __value = std::move(__result.value());
      return __value;
    }
    inline void stopRanging(const std::string& peerId) override {
      auto __result = _swiftPart.stopRanging(peerId);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline void setTransportMode(const std::string& mode) override {
      auto __result = _swiftPart.setTransportMode(mode);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline std::function<void()> addEventListener(const std::function<void(const ProximityEvent& /* event */)>& handler) override {
      auto __result = _swiftPart.addEventListener(handler);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
      auto __value = std::move(__result.value());
      return __value;
    }

  private:
    SolidarityProximity::HybridProximitySpec_cxx _swiftPart;
  };

} // namespace margelo::nitro::solidarity::proximity
