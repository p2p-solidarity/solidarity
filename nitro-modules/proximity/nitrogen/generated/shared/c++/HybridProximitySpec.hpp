///
/// HybridProximitySpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `ProximityEvent` to properly resolve imports.
namespace margelo::nitro::margelo::solidarity::proximity { struct ProximityEvent; }

#include <string>
#include <NitroModules/Promise.hpp>
#include <NitroModules/ArrayBuffer.hpp>
#include <functional>
#include "ProximityEvent.hpp"

namespace margelo::nitro::margelo::solidarity::proximity {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `Proximity`
   * Inherit this class to create instances of `HybridProximitySpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridProximity: public HybridProximitySpec {
   * public:
   *   HybridProximity(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridProximitySpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridProximitySpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridProximitySpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual void startAdvertising(const std::string& displayName, const std::string& serviceType, const std::string& discoveryInfoJson) = 0;
      virtual void stopAdvertising() = 0;
      virtual void startBrowsing(const std::string& serviceType) = 0;
      virtual void stopBrowsing() = 0;
      virtual std::shared_ptr<Promise<bool>> invitePeer(const std::string& peerId, const std::shared_ptr<ArrayBuffer>& payload, double timeoutSec) = 0;
      virtual void acceptInvitation(const std::string& peerId) = 0;
      virtual void rejectInvitation(const std::string& peerId) = 0;
      virtual std::shared_ptr<Promise<void>> sendData(const std::string& peerId, const std::shared_ptr<ArrayBuffer>& data) = 0;
      virtual void disconnect(const std::string& peerId) = 0;
      virtual std::shared_ptr<Promise<void>> startRanging(const std::string& peerId) = 0;
      virtual void stopRanging(const std::string& peerId) = 0;
      virtual std::function<void()> addEventListener(const std::function<void(const ProximityEvent& /* event */)>& handler) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "Proximity";
  };

} // namespace margelo::nitro::margelo::solidarity::proximity
