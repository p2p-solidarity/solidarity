<?xml version="1.0" encoding="utf-8"?>
<!--
  AndroidManifest.xml
  @solidarity/nitro-cloudkit (Android)

  Minimal — only declares the package. Runtime permissions (`INTERNET`
  for Drive REST calls) must be declared by the consuming app (already
  in apps/expo/app.json).
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android" />
